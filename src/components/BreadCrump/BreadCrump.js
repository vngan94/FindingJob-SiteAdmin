import classNames from "classnames/bind";

import styles from "./BreadCrump.module.scss";

const cx = classNames.bind(styles);

function BreadCrump() {
  return <div className={cx("BreadCrumpContainer")}>

  </div>
}

export default BreadCrump;
